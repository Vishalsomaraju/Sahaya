{
  "name": "sahaya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix server",
    "start": "npm run start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run db:studio --prefix server\"",
    "db:push": "npm run db:push --prefix server",
    "db:studio": "npm run db:studio --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
